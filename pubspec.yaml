name: food_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

workspace:
  - packages/core
  - packages/design_system
  - packages/models
  - packages/i18n
  - features/notification
  - features/location
  - features/payment
  - features/auth
  - features/review
  - features/profile
  - features/customer_experience

melos:
  scripts:
    analyze:
      run: melos exec -- flutter analyze .
      description: Run flutter analyze in every package.
    test:
      run: melos exec --dir-exists=test -- flutter test
      description: Run flutter test in every package that has one.

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Local monorepo packages (workspace + melos config below)
  core:
    path: packages/core
  design_system:
    path: packages/design_system
  models:
    path: packages/models
  i18n:
    path: packages/i18n
  notification:
    path: features/notification
  location:
    path: features/location
  payment:
    path: features/payment
  auth:
    path: features/auth
  review:
    path: features/review
  profile:
    path: features/profile
  customer_experience:
    path: features/customer_experience

  # State management & navigation
  get: ^4.6.6
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^13.2.0

  # HTTP & API
  dio: ^5.4.3
  retrofit: ^4.1.0 # optional, adds type-safe API layer

  # Secure storage (JWT token)
  flutter_secure_storage: ^9.0.0

  # Local config / env
  flutter_dotenv: ^5.1.0

  # Maps & location
  google_maps_flutter: ^2.6.1
  geolocator: ^11.0.0

  # Images
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.1
  image_picker: ^1.1.2

  # QR code display (pickup code)
  qr_flutter: ^4.1.0

  # UI helpers
  intl: ^0.19.0 # date/time formatting
  equatable: ^2.0.5 # value equality for models
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  firebase_messaging: ^16.2.2

  # Push notifications
  flutter_client_sse: ^2.0.0

  # JWT
  jwt_decoder: ^2.0.1

  # URL launching (maps, external links)
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.4.9
  riverpod_generator: ^2.4.0
  json_serializable: ^6.8.0
  flutter_launcher_icons: ^0.14.3
  melos: ^7.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # fonts:
  #   - family: Nunito
  #     fonts:
  #       - asset: fonts/Nunito-Regular.ttf
  #       - asset: fonts/Nunito-SemiBold.ttf  weight: 600
  #       - asset: fonts/Nunito-Bold.ttf      weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  assets:
    - .env
    - assets/icon/perka_mark_light.png
    - assets/icon/perka_mark_dark.png
    - assets/dashboard/food_banner/
    - assets/dashboard/cosmetics_banner/
  fonts:
    - family: Nunito
      fonts:
        - asset: fonts/Nunito-VariableFont_wght.ttf
        - asset: fonts/Nunito-Italic-VariableFont_wght.ttf
          weight: 700
          style: normal

    # - family: Trajan Pro
    #   fonts:
    #     - asset: fonts/TrajanPro.ttf
    #     - asset: fonts/TrajanPro_Bold.ttf
    #       weight: 700
    #       style: italic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  image_path: "assets/icon/perka_icon.png"
  android: true
  ios: true
  adaptive_icon_background: "#013C39"
  adaptive_icon_foreground: "assets/icon/perka_adaptive_foreground.png"
  remove_alpha_ios: true
