name: location
description: Address geolocation, distance calculation, and opening external maps.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  geolocator: ^11.0.0
  url_launcher: ^6.3.1
  core:
    path: ../../packages/core
  design_system:
    path: ../../packages/design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
